# This folder is meant for the photos you want to analyze
# (the "Add Images" dialog opens it directly).
#
# The rule below keeps the folder in Git but ignores
# any content (your photos stay local):
*
!.gitignore
